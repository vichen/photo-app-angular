angular.module('app')
  .controller('InputFormController', function($scope) {
    $scope.artist;
    $scope.imageUrl;
    $scope.title;

    $scope.addImage = function(title, url, artist) {
      
    }
  })